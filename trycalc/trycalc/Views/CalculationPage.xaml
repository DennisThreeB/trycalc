<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:trycalc.ViewModels;assembly=trycalc"
             mc:Ignorable="d"
             x:Class="trycalc.Views.CalculationPage"
             NavigationPage.HasNavigationBar="true">

    <ContentPage.Content>
<StackLayout>
    <Grid IsVisible="True" x:Name="ValueBetGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="70"/>
        </Grid.ColumnDefinitions>

            <Picker Grid.Row="0" Grid.ColumnSpan="2" Title="Calculation options" HorizontalOptions="CenterAndExpand" SelectedItem="{Binding SelectedCalculation, Mode=TwoWay}" ItemsSource="{Binding CalculationOptions}"></Picker>

            <Label Grid.Column="0" Grid.Row="1" Text="Type in the quote of your bookie"/>
            <Entry Grid.Row="1" Grid.Column="1" Placeholder="1,23" Keyboard="Numeric" x:Name="bookieQuoteEntry" Text="{Binding BetData.bookieQuote, Mode=TwoWay}"/>

            <Label Grid.Column="0" Grid.Row="2" Text="Type in your estimated probability (in %)"/>
            <Entry Grid.Row="2" Grid.Column="1" Placeholder="30" Keyboard="Numeric" x:Name="estimatedProbabilityEntry" Text="{Binding BetData.estimatedProbability, Mode=TwoWay}"/>

            <Button Grid.Row="3" Grid.ColumnSpan="2" Text="Calculate !" Clicked="CalculateValueBetClicked"/>

            <Label Grid.Row="4" Grid.ColumnSpan="2" HorizontalOptions="Center" FontAttributes="Bold" x:Name="ValueLabel"/>
</Grid>

<Grid IsVisible="False" x:Name="PoissonGrid">

</Grid>
</StackLayout>
    </ContentPage.Content>



</ContentPage>